# cryptography is pinned to 43.0.3 to avoid a build failure on ubuntu-latest
# (GitHub Actions moved ubuntu-latest to 24.04 which ships OpenSSL 3.x).
# The cryptography package >= 44.x has a wheel incompatibility with this
# combination that causes pip install to fail during Kudu/Oryx build.
# See: https://github.com/Azure/azure-sdk-for-python/issues/38725
# Remove the pin once the upstream issue is resolved.
cryptography==43.0.3

azure-functions>=1.18.0
azure-communication-callautomation>=1.1.0
azure-identity>=1.15.0
azure-keyvault-secrets>=4.7.0
azure-appconfiguration>=1.5.0
msgraph-sdk>=1.0.0
rapidfuzz>=3.6.0
jellyfish>=1.0.0
azure-monitor-opentelemetry>=1.2.0
